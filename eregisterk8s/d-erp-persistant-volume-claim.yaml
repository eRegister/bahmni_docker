apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: erp-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  storageClassName: eregister-storage-class
  volumeName: erp-persistent-volume
